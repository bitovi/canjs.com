<h4>CDN:</h4>
<div class="input">
  <select>
    {{#libraries}}
    <option value="{{id}}">{{description}}</option>
    {{/libraries}}
  </select>
  <span>//cdn.canjs.com/releases/{{version}}/can.{{selectedLibrary}}.js</span>
</div>