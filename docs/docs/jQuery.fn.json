c({"name": "jQuery.fn", "description": "<p><p>undefined</p> Called on a jQuery collection that was rendered with can.View with pending hookups.  can.View can render a \n template with hookups, but not actually perform the hookup, because it returns a string without actual DOM \n elements to hook up to.  So hookup performs the hookup and clears the pending hookups, preventing errors in \n future templates.</p>\n\n<pre>$(can.View('//views/recipes.ejs',recipeData)).hookup()</pre> ", "src": "can/view/modifiers/modifiers.js", "line": 142, "parent": "can.View"})